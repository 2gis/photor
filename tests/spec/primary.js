describe('Только с основными параметрами', function() {
    // before(function() {
    //     $('.photor').photor({
    //         control: defaultParams.control,
    //         next: defaultParams.next,
    //         prev: defaultParams.prev,
    //         thumb: defaultParams.thumb,
    //         thumbImg: defaultParams.thumbImg,
    //         viewport: defaultParams.viewport,
    //         layer: defaultParams.layer,
    //         slide: defaultParams.slide
    //     });
    // });

    // common();

    // it('Выставился data-атрибут', function() {
    //     assert(true);
    // });

    // after(function() {
    //     // dispose
    // });
});